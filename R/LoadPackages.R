library(devtools)
library(datesets)

#Error --- not necessary in course though?
library(slidify)


library(ggplot2)


library(jsonlite)
library(XML)

#Must install the appropriate version of Java for the chosen version of R
library(rJava)
library(xlsx)

library(httr)
library(data.table)
library(RMySQL)

library(kernlab)
library(boot)

library(MASS)

library(knitr)
library(xtable)

library(lattice)
library(nlme)

library(Hmisc)
library(reshape2)
library(plyr)

library(jpeg)
library(quantmod)

library(shiny)
library(shinyapps)
# On first run, make sure you get the secret from the following site:
# https://www.shinyapps.io/dashboard

source("http://bioconductor.org/biocLite.R")
library(GenomicFeatures)
library(AnnotationDbi)
library(rhdf5)
